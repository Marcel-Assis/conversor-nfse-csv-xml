# Bytecode e cache do Python
__pycache__/
*.pyc

# Artefatos do PyInstaller (gerados a cada build — não precisam ir pro repo)
build/
dist/
*.spec

# XMLs gerados pelo conversor (dados reais de notas fiscais — não devem ir pro repo)
*.xml
!exemplos/*.xml

# Ambientes virtuais, se você usar
.venv/
venv/

# Arquivos de sistema
.DS_Store
Thumbs.db